name: Trigger auto deployment for container-app-ebda524pfichu

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-ebda524pfichu-AutoDeployTrigger-521a408d-b4de-4fc1-a64d-9bb23fd08a22.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPEBDA524PFICHU_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPEBDA524PFICHU_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPEBDA524PFICHU_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPEBDA524PFICHU_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPEBDA524PFICHU_REGISTRY_PASSWORD }}
          containerAppName: container-app-ebda524pfichu
          resourceGroup: expresssampleb6eaba39
          imageToBuild: default/container-app-ebda524pfichu:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


